{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "현재파일 디버깅 (CodeLLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}.out",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "현재파일 빌드하기",
            "terminal": "integrated"
        }
    ]
}